# nkmexpert

Чистый стартовый проект на `Vite + React + TypeScript`.

## Запуск

Установка зависимостей:

```bash
pnpm install
```

Запуск dev-сервера:

```bash
pnpm dev
```

Открой адрес, который покажет консоль, обычно `http://localhost:5173`.
