# nkmexpert

Чистый стартовый проект на **Vite + React + TypeScript**.

## Запуск (live dev server)

Если зависимости ещё не установлены:

```bash
pnpm install
```

Запуск для разработки:

```bash
pnpm dev
```

Открой в браузере адрес, который покажет консоль (обычно `http://localhost:5173`).
